<?xml version="1.0" encoding="iso-8859-1"?>

<project name="sourcerer-idcs" default="build">

	<!-- Propriedades -->
	<property name="web" location="../WebContent" />
	<property name="dist" location="../dist" />

	<!-- Build do aplicativo -->
	<target name="build">
		<war destfile="${dist}/sourcerer-idcs.war" webxml="${web}/WEB-INF/web.xml" basedir="${web}" />
	</target>

</project>